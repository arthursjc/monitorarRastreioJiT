name: jt-tracker

on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: jt-tracker
  cancel-in-progress: false

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install deps
        run: pip install requests curl_cffi playwright

      - name: Run tracker
        env:
          WAYBILL_NO: ${{ vars.WAYBILL_NO }}
          WAYBILL_LABELS: ${{ vars.WAYBILL_LABELS }}
          CALLMEBOT_PHONE: "5512988416345"
          CPF: ${{ secrets.CPF }}
          CALLMEBOT_APIKEY: ${{ secrets.CALLMEBOT_APIKEY }}
          JADLOG_CTE: ${{ vars.JADLOG_CTE }}
          JADLOG_LABELS: ${{ vars.JADLOG_LABELS }}
          CORREIOS_CODES: ${{ vars.CORREIOS_CODES }}
          CORREIOS_LABELS: ${{ vars.CORREIOS_LABELS }}
        run: python track.py

      - name: Commit state se mudou
        run: |
          git config user.name  "jt-tracker-bot"
          git config user.email "jt-tracker-bot@users.noreply.github.com"
          if [[ -n "$(git status --porcelain state/)" ]]; then
            git add state/
            git commit -m "chore: atualiza state [skip ci]"
            git push
          else
            echo "sem mudancas no state"
          fi
